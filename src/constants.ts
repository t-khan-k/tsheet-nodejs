export const baseUrl = 'https://rest.tsheets.com/api';
export const version = 'v1';
export const url = baseUrl + '/' + version;
export const authUrl = url + '/authorize';
export const userUrl = url + '/users';
export const groupUrl = url + '/groups';